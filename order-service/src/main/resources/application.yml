server:
  port: 8083
  servlet:
    context-path: /api/v1

spring:
  application:
    name: order-service
  data:
    mongodb:
      uri: ${MONGODB_URI:mongodb+srv://Ketan:krshna901@med-pharm.nl4u9wk.mongodb.net/orders_db?appName=Med-Pharm}
      database: orders_db
  jackson:
    default-property-inclusion: non_null
    serialization:
      write-dates-as-timestamps: false

jwt:
  secret: ${JWT_SECRET:cf01494b904ba015b00426b898ed3fe5668ece8914d91e26846f0a6f4addc2a3}

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics,prometheus
  endpoint:
    health:
      probes:
        enabled: true

springdoc:
  api-docs:
    path: /api-docs
  swagger-ui:
    path: /swagger-ui.html

logging:
  level:
    com.edublitz: INFO

cors:
  allowed-origins: ${CORS_ALLOWED_ORIGINS:*}

services:
  product-service:
    base-url: ${PRODUCT_SERVICE_URL:http://localhost:8082/api/v1}
  user-service:
    base-url: ${USER_SERVICE_URL:http://localhost:8081/api/v1}
